import { Address } from "../../../common/src/address.js";
import { AvmValue } from "./avm-value.js";

//#region packages/algod_client/src/models/application-state-operation.d.ts
/**
 * An operation against an application's global/local/box state.
 */
type ApplicationStateOperation = {
  /**
   * Operation type. Value `w` is **write**, `d` is **delete**.
   */
  operation: string;
  /**
   * Type of application state. Value `g` is **global state**, `l` is **local state**, `b` is **boxes**.
   */
  appStateType: string;
  /**
   * The key (name) of the global/local/box state.
   */
  key: Uint8Array;
  newValue?: AvmValue;
  /**
   * For local state changes, the address of the account associated with the local state.
   */
  account?: Address;
};
//#endregion
export { ApplicationStateOperation };
//# sourceMappingURL=application-state-operation.d.ts.map