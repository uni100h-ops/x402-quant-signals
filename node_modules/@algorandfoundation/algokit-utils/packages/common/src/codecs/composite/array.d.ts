import { Address } from "../../address.js";
import { Codec } from "../codec.js";
import { EncodingFormat } from "../types.js";

//#region packages/common/src/codecs/composite/array.d.ts
/**
 * Array codec - encodes each element using the item codec
 */
declare class ArrayCodec<T, TEncoded = T> extends Codec<T[], TEncoded[]> {
  private readonly itemCodec;
  constructor(itemCodec: Codec<T, TEncoded>);
  defaultValue(): T[];
  protected toEncoded(value: T[], format: EncodingFormat): TEncoded[];
  protected fromEncoded(value: TEncoded[], format: EncodingFormat): T[];
  isDefaultValue(value: T[]): boolean;
}
declare const bytesArrayCodec: ArrayCodec<Uint8Array<ArrayBufferLike>, string | Uint8Array<ArrayBufferLike>>;
declare const addressArrayCodec: ArrayCodec<Address, string | Uint8Array<ArrayBufferLike>>;
declare const bigIntArrayCodec: ArrayCodec<bigint, number | bigint>;
declare const numberArrayCodec: ArrayCodec<number, number>;
declare const booleanArrayCodec: ArrayCodec<boolean, boolean>;
declare const stringArrayCodec: ArrayCodec<string, string | Uint8Array<ArrayBufferLike>>;
//#endregion
export { ArrayCodec, addressArrayCodec, bigIntArrayCodec, booleanArrayCodec, bytesArrayCodec, numberArrayCodec, stringArrayCodec };
//# sourceMappingURL=array.d.ts.map