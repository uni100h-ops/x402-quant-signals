import { Codec } from "../codec.js";
import { EncodingFormat } from "../types.js";
import { WireObject } from "../wire.js";

//#region packages/common/src/codecs/composite/record.d.ts
/**
 * Record codec - for string-keyed objects with homogeneous values
 */
declare class RecordCodec<V, VEncoded = V> extends Codec<Record<string, V>, Record<string, VEncoded>, WireObject<VEncoded>> {
  private readonly valueCodec;
  constructor(valueCodec: Codec<V, VEncoded>);
  defaultValue(): Record<string, V>;
  protected toEncoded(value: Record<string, V>, format: EncodingFormat): Record<string, VEncoded>;
  protected fromEncoded(value: WireObject<VEncoded>, format: EncodingFormat): Record<string, V>;
  isDefaultValue(value: Record<string, V>): boolean;
}
//#endregion
export { RecordCodec };
//# sourceMappingURL=record.d.ts.map