import { Codec } from "../codec.js";
import { EncodingFormat, ObjectModelMetadata } from "../types.js";
import { WireObject } from "../wire.js";

//#region packages/common/src/codecs/models/object-model.d.ts
declare class ObjectModelCodec<T extends Record<string, unknown> = Record<string, unknown>> extends Codec<T, Record<string, unknown>, WireObject> {
  private readonly metadata;
  private resolvedMetadata;
  private resolvedDefaultValue;
  constructor(metadata: ObjectModelMetadata<T> | (() => ObjectModelMetadata<T>));
  decode(value: WireObject | undefined | null, format: EncodingFormat): T;
  private getMetadata;
  defaultValue(): T;
  isDefaultValue(value: T): boolean;
  protected toEncoded(value: T, format: EncodingFormat): Record<string, unknown>;
  protected fromEncoded(value: WireObject, format: EncodingFormat): T;
  private encodeFlattenedField;
  private decodeFieldValue;
}
//#endregion
export { ObjectModelCodec };
//# sourceMappingURL=object-model.d.ts.map