const require_codec = require("../codec.js");
//#region packages/common/src/codecs/primitives/bigint.ts
var BigIntCodec = class extends require_codec.Codec {
	defaultValue() {
		return 0n;
	}
	fromEncoded(value, _format) {
		if (typeof value === "bigint") return value;
		if (typeof value === "number" || typeof value === "string") return BigInt(value);
		throw new Error(`Cannot decode bigint from ${typeof value}`);
	}
};
const bigIntCodec = new BigIntCodec();
//#endregion
exports.bigIntCodec = bigIntCodec;

//# sourceMappingURL=bigint.js.map