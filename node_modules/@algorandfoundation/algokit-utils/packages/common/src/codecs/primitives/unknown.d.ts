import { Codec } from "../codec.js";
import { EncodingFormat } from "../types.js";

//#region packages/common/src/codecs/primitives/unknown.d.ts
/**
 * Unknown codec - passthrough for unknown/any types
 * Converts Maps with Uint8Array keys to objects with string keys recursively
 */
declare class UnknownCodec extends Codec<unknown, unknown> {
  private textDecoder;
  private recordCodec;
  private arrayCodec;
  constructor();
  defaultValue(): unknown;
  protected toEncoded(_value: unknown, _format: EncodingFormat): unknown;
  protected fromEncoded(value: unknown, format: EncodingFormat): unknown;
  private maybeDecodeAsUtf8;
  /**
   * Recursively process unknown values, using various codecs where appropriate
   */
  private processValue;
}
declare const unknownCodec: UnknownCodec;
//#endregion
export { unknownCodec };
//# sourceMappingURL=unknown.d.ts.map