import { IntMode, decode, encode } from "algorand-msgpack";
//#region packages/common/src/msgpack.ts
function decodeMsgpack(buffer, options = {
	useMap: true,
	rawBinaryStringKeys: true,
	rawBinaryStringValues: true
}) {
	return decode(buffer, {
		intMode: IntMode.AS_ENCODED,
		...options
	});
}
function encodeMsgpack(data) {
	return new Uint8Array(encode(data, {
		sortKeys: true,
		ignoreUndefined: true
	}));
}
//#endregion
export { decodeMsgpack, encodeMsgpack };

//# sourceMappingURL=msgpack.mjs.map