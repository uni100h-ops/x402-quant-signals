import { Address } from "../../../common/src/address.js";
import { BoxReference } from "./box-reference.js";
import { HoldingRef } from "./holding-ref.js";
import { LocalsRef } from "./locals-ref.js";

//#region packages/indexer_client/src/models/resource-ref.d.ts
/**
 * ResourceRef names a single resource. Only one of the fields should be set.
 */
type ResourceRef = {
  /**
   * \[d\] Account whose balance record is accessible by the executing ApprovalProgram or ClearStateProgram.
   */
  address?: Address;
  /**
   * \[p\] Application id whose GlobalState may be read by the executing
   * ApprovalProgram or ClearStateProgram.
   */
  applicationId?: bigint;
  /**
   * \[s\] Asset whose AssetParams may be read by the executing
   * ApprovalProgram or ClearStateProgram.
   */
  assetId?: bigint;
  box?: BoxReference;
  holding?: HoldingRef;
  local?: LocalsRef;
};
//#endregion
export { ResourceRef };
//# sourceMappingURL=resource-ref.d.ts.map