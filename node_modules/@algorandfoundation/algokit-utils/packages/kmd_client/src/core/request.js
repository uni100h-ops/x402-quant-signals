const require_json = require("../../../common/src/json.js");
const require_msgpack = require("../../../common/src/msgpack.js");
const require_base_http_request = require("./base-http-request.js");
const require_api_error = require("./api-error.js");
//#region packages/kmd_client/src/core/request.ts
const encodeURIPath = (path) => encodeURI(path).replace(/%5B/g, "[").replace(/%5D/g, "]");
async function request(config, options) {
	let rawPath = options.url.endsWith("/") ? options.url.slice(0, -1) : options.url;
	if (options.path) for (const [key, value] of Object.entries(options.path)) {
		const replace = encodeURIPath(require_base_http_request.inputValueAsString(value));
		rawPath = rawPath.replace(`{${key}}`, replace);
	}
	const url = new URL(rawPath, config.baseUrl);
	if (config.port !== void 0) url.port = config.port.toString();
	if (options.query) for (const [key, value] of Object.entries(options.query)) {
		if (value === void 0 || value === null) continue;
		url.searchParams.set(key, Array.isArray(value) ? value.map((v) => require_base_http_request.inputValueAsString(v)).join(",") : require_base_http_request.inputValueAsString(value));
	}
	const headers = {
		...config.headers ?? {},
		...options.headers ?? {}
	};
	if (config.token) if (typeof config.token === "string") headers["X-KMD-API-Token"] = config.token;
	else for (const [name, value] of Object.entries(config.token)) headers[name] = value;
	const requestContentType = options.headers?.["Content-Type"] ?? options.headers?.["content-type"];
	let bodyPayload = void 0;
	if (options.body != null) if (options.body instanceof Uint8Array) bodyPayload = options.body.slice().buffer;
	else if (typeof options.body === "string") bodyPayload = options.body;
	else if (requestContentType?.includes("msgpack")) bodyPayload = require_msgpack.encodeMsgpack(options.body).slice().buffer;
	else if (requestContentType?.includes("json")) bodyPayload = require_json.stringifyJson(options.body);
	else bodyPayload = require_json.stringifyJson(options.body);
	const response = await fetch(url.toString(), {
		method: options.method,
		headers,
		body: bodyPayload
	});
	const responseContentType = response.headers.get("content-type") ?? "";
	if (!response.ok) {
		let errorBody;
		try {
			if (responseContentType.includes("application/msgpack")) errorBody = require_msgpack.decodeMsgpack(new Uint8Array(await response.arrayBuffer()), {
				useMap: false,
				rawBinaryStringKeys: false,
				rawBinaryStringValues: false
			});
			else if (responseContentType.includes("application/json")) errorBody = require_json.parseJson(await response.text());
			else errorBody = await response.text();
		} catch {
			errorBody = void 0;
		}
		throw new require_api_error.ApiError(url.pathname.toString(), response.status, errorBody);
	}
	if (responseContentType.includes("application/msgpack") || responseContentType.includes("application/octet-stream") || responseContentType.includes("application/x-binary")) return new Uint8Array(await response.arrayBuffer());
	if (responseContentType.includes("application/json")) return require_json.parseJson(await response.text());
	if (!responseContentType) return new Uint8Array(await response.arrayBuffer());
	return await response.text();
}
//#endregion
exports.request = request;

//# sourceMappingURL=request.js.map