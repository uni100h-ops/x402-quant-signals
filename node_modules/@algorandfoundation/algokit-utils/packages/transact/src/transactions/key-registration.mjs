import { TransactionValidationErrorType } from "./common.mjs";
//#region packages/transact/src/transactions/key-registration.ts
/**
* Validate key registration transaction fields
*/
function validateKeyRegistrationTransaction(keyReg) {
	const errors = new Array();
	if (keyReg.voteKey !== void 0 || keyReg.selectionKey !== void 0 || keyReg.stateProofKey !== void 0 || keyReg.voteFirst !== void 0 || keyReg.voteLast !== void 0 || keyReg.voteKeyDilution !== void 0) errors.push(...validateOnlineKeyRegistration(keyReg));
	return errors;
}
/**
* Validate online key registration fields
*/
function validateOnlineKeyRegistration(keyReg) {
	const errors = new Array();
	if (!keyReg.voteKey) errors.push({
		type: TransactionValidationErrorType.RequiredField,
		data: "Vote key"
	});
	if (!keyReg.selectionKey) errors.push({
		type: TransactionValidationErrorType.RequiredField,
		data: "Selection key"
	});
	if (!keyReg.stateProofKey) errors.push({
		type: TransactionValidationErrorType.RequiredField,
		data: "State proof key"
	});
	if (keyReg.voteFirst === void 0) errors.push({
		type: TransactionValidationErrorType.RequiredField,
		data: "Vote first"
	});
	if (keyReg.voteLast === void 0) errors.push({
		type: TransactionValidationErrorType.RequiredField,
		data: "Vote last"
	});
	if (keyReg.voteFirst !== void 0 && keyReg.voteLast !== void 0 && keyReg.voteFirst >= keyReg.voteLast) errors.push({
		type: TransactionValidationErrorType.ArbitraryConstraint,
		data: "Vote first must be less than vote last"
	});
	if (keyReg.voteKeyDilution === void 0) errors.push({
		type: TransactionValidationErrorType.RequiredField,
		data: "Vote key dilution"
	});
	if (keyReg.nonParticipation === true) errors.push({
		type: TransactionValidationErrorType.ArbitraryConstraint,
		data: "Online key registration cannot have non participation flag set"
	});
	return errors;
}
//#endregion
export { validateKeyRegistrationTransaction };

//# sourceMappingURL=key-registration.mjs.map