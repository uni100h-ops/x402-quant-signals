import JSONRequest from '../jsonrequest.js';
import { decodeJSON } from '../../../encoding/encoding.js';
import { AccountAssetsInformationResponse } from './models/types.js';
export default class AccountAssetsInformation extends JSONRequest {
    constructor(c, account) {
        super(c);
        this.account = account.toString();
    }
    path() {
        return `/v2/accounts/${this.account}/assets`;
    }
    limit(limit) {
        this.query.limit = limit;
        return this;
    }
    next(next) {
        this.query.next = next;
        return this;
    }
    // eslint-disable-next-line class-methods-use-this
    prepare(response) {
        return decodeJSON(response.getJSONText(), AccountAssetsInformationResponse);
    }
}
//# sourceMappingURL=accountAssetsInformation.js.map